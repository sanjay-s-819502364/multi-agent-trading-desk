# Project instructions

## Git

- Stage changes with `git add` when asked to track/save files, but never run `git commit` — the user commits themselves.

## Code style

- Never add code comments unless absolutely necessary (a non-obvious constraint, invariant, or workaround). Do not explain what code does — well-named code should speak for itself.
